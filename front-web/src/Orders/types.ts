export type Product = {
  id: number,
  name: string,
  price: number,
  description: number,
  imageUri: string;
}

export type Orderlocationdata = {
  address: string,
  latitude: number,
  longitude: number,
}